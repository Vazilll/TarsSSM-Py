# TARS v3 — Core Dependencies
# Install: pip install -r requirements.txt

# ═══ Обязательные (ядро) ═══
torch
numpy
einops
tqdm

# ═══ Токенайзеры ═══
sentencepiece
tokenizers

# ═══ Голос (STT + TTS) ═══
faster-whisper
sounddevice
# piper-tts  # Опционально, для голосового синтеза

# ═══ RAG и память (LEANN) ═══
sentence-transformers

# ═══ Датасеты HuggingFace ═══
datasets

# ═══ Опционально ═══
# onnxruntime        # Для Silero VAD ONNX
# pyttsx3            # Системный голос (fallback)
# duckduckgo-search  # Веб-поиск
